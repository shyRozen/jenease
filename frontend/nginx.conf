server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — all routes serve index.html
    # API calls are handled upstream by Apache ProxyPass /api → backend
    location / {
        try_files $uri $uri/ /index.html;
    }
}
